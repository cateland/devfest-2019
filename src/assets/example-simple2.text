// Je veut ecouter une chanson

Just(👨‍🎤).fold(
    () => A , // on Nothing
    (👨‍🎤) => A' // on Just
)
// => A => le chanteur chante

Nothing.fold(
    () => A , // on Nothing
    (👨‍🎤) => A' // on Just
)
// => A' => pas de chanteur je lance spotify